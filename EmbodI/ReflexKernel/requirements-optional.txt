# Optional feature groups for ReflexKernel
# Install with: pip install -r requirements-optional.txt  or  pip install -e .[viz,vision,...]

# Visualization (recommended for demos)
pygame>=2.5

# Computer Vision + body tracking
opencv-python>=4.8
mediapipe>=0.10.0

# Audio (onset / energy / future VAD)
sounddevice>=0.4
# pyaudio ; platform_system != "Windows"   # alternative

# ML / Embeddings / Sentiment (Thought Bridge advanced path)
sentence-transformers>=2.2
scikit-learn>=1.3
# torch ; cpu only is usually sufficient — install manually if needed: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Optional server interface
fastapi>=0.100
uvicorn[standard]>=0.23
websockets>=11.0
httpx>=0.24

# Dev / testing
pytest>=7.4
pytest-asyncio>=0.21
mypy>=1.5
ruff>=0.1
black>=23.0
